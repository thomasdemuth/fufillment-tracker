services:
  app:
    build: .
    image: fulfillment-tracker:local
    ports:
      - "8000:8000"
    env_file:
      - path: .env
        required: false
    environment:
      DATA_DIR: /data
    volumes:
      - ./data:/data
    restart: unless-stopped
